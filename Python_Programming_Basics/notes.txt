I learned from the Very Cool Book:
    Automate the Boring Stuff with Python
        Al Sweigart

This is the beginning of my journey of :
    Learning Python Full Fledgedly..

In this folder: Python_Programming_Basics contains

Part I: Python Programming Basics

Chapter 1: Python Basics
    1. Expression
    Expression => contains => values such as 2 and operators such as + and they can always evaluate (that is reduce) to a single value.
    I.E Expression can be used anywhere where there consist the value.
    example: 2 + 2 => 4 which is single value.
    A single value with no operators is also considered as expression, though it evaluates itself.
    example: 2 => 2

    Errors are okay! thats how you learn.

    2. Math Operators from Highest to Lowest Precedence

    Operator    Operation                           Example     Evaluates to...
    **          Exponent                            2 ** 3      8
    %           Modules/remainder                   22 % 8      6
    //          Integer devision/ floored quotient  22 // 8     2
    /           Devision                            22 / 8      2.75
    *           Multiplication                      3 * 5       15
    -           Subtraction                         5 - 2       3
    +           Addition                            2 + 2       4


    3. Precedence

    ** -> *, /, // and % (from left to right) -> + and - (from left to right)
    use parenthesis () to override the usual precedence.
    example: 2 + 3 * 6 / (2 + 3) * 6 => 20 / 30 respectively 

    4. Data Types
        The Integer
            eg : -2, -1, 0, 1, 2, 3, 4, 5, etc
        Floating Point
            eg: -1.25, -1.0, -0.5, 0.0, 0.5, 1.0, 1.25, etc
        String
            eg: 'a', 'aa', 'aaa', 'Hello!', '11 cats', etc
            text values called strings, or strs
            Always surround string with '' or "" as in
            "Hello" or "Hello bro this is heaven you have died.."
            stings with no character init '' is called blanked string
            if ' is missing errors shows EOL while scanning string literal
            eg 'hello


    5. String Concatenation and replication

        Concatenation (When + operator is used in string it is called concatenation cuz it joins the strings)
            "Arbin" + " " + "Bhasima"
            Arbin Bhasima

            if 
            'Arbin' + 777
            This throws error becuase strings and integers cannot be concatenate nor add
             error msg says : 
                Can't convert 'int' object ro str implicitly

        Replication (when * operator is used the string is replicated)
            "Arbin" * 2
            ArbinArbin

            two strings cannot be replicated 
            'bob' * 'marley'
            this throws error :
            can't multiply sequence by non-int of type 'str'

            String and float cannot be multiplied as well 
            'Alice' * 5.0
            can't multiply sequence by non-int of type 'float'

    6. Storing Values in Variables
        Variable is like a box in the computers memory where you can store a single value.
        If you want to use the result of an evaluated expression later in program, you can save it inside a variable.

            Assignment Statementts
                An Assignment Statementts consists of a vaiable name, an eual sign and the value to be stored.
                syntax is :
                    Variable_name = value
                for eg: 
                    Name = "Arbin"
                    age = 23
            
            Variable Names
                Only one word:
                    last name : this is not correct
                    lastname
                    last_name
                variable name cannot be started as number
                
                Valid variable names    Invalid Variable names
                balance                 Current-balance (hyphens (-) are not allowed)
                currentbalance          current balance (spaces are not allowed)
                current_balance         4account (can't begin with number)
                _spam                   42 (can't begin with number)
                SPAM                    total_$um (Special characters like $ are not allowed)
                account4                'hello' (special characters like ' are not allowed)


            spam, SPAM, Spam, sPaM are four different variables name

            python ma chain naming conventions haru jailai nai small case bata suru hunxa 
            
            underscore _ kina kam lagxa

            for e.g:
            looking_like_this
            date_of_birth
            lots_of_love
            laughing_out_loud
            _lol
            _dob

            a good variable names describes what data is stored in them...

    7. Create a first Program
        hello world -> ask user name -> find length of user name -> ask user age -> find the next year age of the user

        Text and number equivalance

        42 == '42'
        False

        42 == 42.0
        True

        42.0 == 0042.000
        True
    
    8. Practice Questions.
    qn 1 to 10 in answers.py file


    Extra Credit : len() , look up round() 

                    


Chapter 2: Flow Control
    2.1 Flow Control Statements 
        Flow Control can decide which python instructions to execute under which conditions
        Flow Control directly correspond to the symbols in a flowchart..

        Flow chart for what to do if it's raining.
        Follow the path made by arrows from start to end:
         
        

                                        no  -> Go Outside -> end
            start ->    Is raining? ->                      yes ->  Go Outside -> end     
                                        yes -> Have Umbrella                                   no -> Go Outside -> end
                                                            no  ->  Wait a While -> is raining?
                                                                                               yes -> wait a while (loop) -> and so on     
            Fig : flowchart

        Before learning Flow Chart Control 

    2.2 Need to Understand Boolean Values (Yes and No) options

        Boolean Data type
        Boolean Values: Boolean Values have only two values that is 
            TRUE  represent as True, yes, 1 and so on
            FALSE represent as False, no, 0 and so on

    2.3 Comparison Operators

        Comparison Operators compare two values and evaluates down to a single Boolean value.
        
        Operator    Meaning
        ==          Equal to
        !=          Not Equal to
        <           Less than
        >           Greater than
        <=          less than or equal to
        >=          Greater than or eual to

        This Operators evaluates to True and False depending on the values you give them...

        Can be use on any data types...

        The Difference Between == AND = operators
            The == operator (equal to) asks whether two values are the same as each other
            The = operator (assignment) puts the value on the right into the variable on the left

    2.4 Boolean Operators
        Binary Boolean Operators
        True and True is True
        True and False is False

        The and Operator's Truth table
        Expression      Evaluates to...
        True and True   True
        True and False  False
        False and True  False
        False and False False

        The or Operator;s Truth Table
        Expression      Evaluates to...
        True or True    True
        True or False   True
        False or True   True
        False or False  False

        The not Operator

        Not True => False
        
        not not not not True => True

        The not Operator's Truth Table

        Expression      Evaluates to...
        Not True        False
        Not False       True

    2.5 Mixing Boolean and Comaparison Operators
        ( 4 < 5 ) and ( 5 > 6 )
        True
        ( 4 < 5 ) and ( 9 < 6 )
        False
        ( 1 == 2 ) or ( 2 == 2 )
        True
        2 + 2 == 4 and not 2 + 2 == 5 and 2 * 2 == 2 + 2
        True

    2.6 Elements of Flow Control

        Flow control statement often starts with a part called the conditions,

        followed by block of code called the clause

        Consitions
            The Boolean expressions could all be considered as conditions which are same thing as expressions

            conditions is just a more specific name in contect of Flow control statement

            Conditions always evaluate down  to a Boolean value
                True
                False

            A Flow Control Statement decides what to do based on whether its condition is True or False
            and almost every flow control statement uses a condition


        Blocks of code
            lines of python code can be grouped together in Blocks
            from indentation of the line of code you can tell when block begins and {% endspaceless %}

                Three Rules for Blocks:
                1. Blocks begin when the indentation increases
                2. Blocks can contain other blocks
                3. Blocks end when the indentation decreases to zero or to a containing blocks indentation
            
            eg:

            if name == 'Bob':
                print('Who?')
            if password == 'swordfish':
                print('Access granted.')
            else:
                print('wrg password')

        Program Execution
            top to down, one after another 
            The program execution is a term for the current instruction being executed

    2.7 Flow Control Statements
        statements represent diamond you see in flow chart true / false, yes / no, etc


        if Statements  
            most common type of flow control statement 
            if statement clause ( the block following the if statement ) will execute 
            if the statement's condition is True.
            The clause is skiped if the condition is False

            in english terminology " If this condition is true, execute the code in the clause "

            if statement consists of :
                the if keyword
                A Condition ( an expression that evaluate to True or False )
                A colon
                Starting on the nexr line, an indented block of code ( called if clause )

                For example:
                    if name == 'Arbin':
                        print('Hi, Boss.')

            The flowchart for an if Statement

                                            True    ->  print('Hi, Boss.')   ->  end
                Start ->    name == 'Arbin' 
                                            False   ->  end

        else Statements
            of clause can optionally be followed by an else statement.
            else clause is executed only when the if statement's condition is False
            in english terminology
                If this condition is true, execute this code. Or else, execute that code

            else statement contains:
                The else keyword
                A colon
                Starting on the next line, an indented block of code ( else clause )

                for example:
                    if name == 'Arbin':
                        print('Hi, Boss.')
                    else:
                        print('Dare yo, Omaye wa!')
            
            The flow chart os this code looks like this

                                        True    ->  print('Hi, Boss.')  ->  end
            Start   ->  name == 'arbin' 
                                        False   ->  print('Dare yo, Omaye wa!') ->  end

        elif Statements






        








Chapter 3: Functions


Chapter 4: Lists


Chapter 5: Dictionaries and Structuring Data


Chapter 6: Manipulating Strings
