I learned from the Very Cool Book:
    Automate the Boring Stuff with Python
        Al Sweigart

This is the beginning of my journey of :
    Learning Python Full Fledgedly..

In this folder: Python_Programming_Basics contains

Part I: Python Programming Basics

Chapter 1: Python Basics
    1. Expression
    Expression => contains => values such as 2 and operators such as + and they can always evaluate (that is reduce) to a single value.
    I.E Expression can be used anywhere where there consist the value.
    example: 2 + 2 => 4 which is single value.
    A single value with no operators is also considered as expression, though it evaluates itself.
    example: 2 => 2

    Errors are okay! thats how you learn.

    2. Math Operators from Highest to Lowest Precedence

    Operator    Operation                           Example     Evaluates to...
    **          Exponent                            2 ** 3      8
    %           Modules/remainder                   22 % 8      6
    //          Integer devision/ floored quotient  22 // 8     2
    /           Devision                            22 / 8      2.75
    *           Multiplication                      3 * 5       15
    -           Subtraction                         5 - 2       3
    +           Addition                            2 + 2       4


    3. Precedence

    ** -> *, /, // and % (from left to right) -> + and - (from left to right)
    use parenthesis () to override the usual precedence.
    example: 2 + 3 * 6 / (2 + 3) * 6 => 20 / 30 respectively 

    4. Data Types
        The Integer
            eg : -2, -1, 0, 1, 2, 3, 4, 5, etc
        Floating Point
            eg: -1.25, -1.0, -0.5, 0.0, 0.5, 1.0, 1.25, etc
        String
            eg: 'a', 'aa', 'aaa', 'Hello!', '11 cats', etc
            text values called strings, or strs
            Always surround string with '' or "" as in
            "Hello" or "Hello bro this is heaven you have died.."
            stings with no character init '' is called blanked string
            if ' is missing errors shows EOL while scanning string literal
            eg 'hello


    5. String Concatenation and replication

        Concatenation (When + operator is used in string it is called concatenation cuz it joins the strings)
            "Arbin" + " " + "Bhasima"
            Arbin Bhasima

            if 
            'Arbin' + 777
            This throws error becuase strings and integers cannot be concatenate nor add
             error msg says : 
                Can't convert 'int' object ro str implicitly

        Replication (when * operator is used the string is replicated)
            "Arbin" * 2
            ArbinArbin

            two strings cannot be replicated 
            'bob' * 'marley'
            this throws error :
            can't multiply sequence by non-int of type 'str'

            String and float cannot be multiplied as well 
            'Alice' * 5.0
            can't multiply sequence by non-int of type 'float'

    6. Storing Values in Variables
        Variable is like a box in the computers memory where you can store a single value.
        If you want to use the result of an evaluated expression later in program, you can save it inside a variable.

            Assignment Statementts
                An Assignment Statementts consists of a vaiable name, an eual sign and the value to be stored.
                syntax is :
                    Variable_name = value
                for eg: 
                    Name = "Arbin"
                    age = 23
            
            Variable Names
                Only one word:
                    last name : this is not correct
                    lastname
                    last_name
                variable name cannot be started as number
                
                Valid variable names    Invalid Variable names
                balance                 Current-balance (hyphens (-) are not allowed)
                currentbalance          current balance (spaces are not allowed)
                current_balance         4account (can't begin with number)
                _spam                   42 (can't begin with number)
                SPAM                    total_$um (Special characters like $ are not allowed)
                account4                'hello' (special characters like ' are not allowed)


            spam, SPAM, Spam, sPaM are four different variables name

            python ma chain naming conventions haru jailai nai small case bata suru hunxa 
            
            underscore _ kina kam lagxa

            for e.g:
            looking_like_this
            date_of_birth
            lots_of_love
            laughing_out_loud
            _lol
            _dob

            a good variable names describes what data is stored in them...

    7. Create a first Program
        hello world -> ask user name -> find length of user name -> ask user age -> find the next year age of the user

        Text and number equivalance

        42 == '42'
        False

        42 == 42.0
        True

        42.0 == 0042.000
        True
    
    8. Practice Questions.
    qn 1 to 10 in answers.py file


    Extra Credit : len() , look up round() 

                    


Chapter 2: Flow Control
    2.1 Flow Control Statements 
        Flow Control can decide which python instructions to execute under which conditions
        Flow Control directly correspond to the symbols in a flowchart..

        Flow chart for what to do if it's raining.
        Follow the path made by arrows from start to end:
         
        

                                        no  -> Go Outside -> end
            start ->    Is raining? ->                      yes ->  Go Outside -> end     
                                        yes -> Have Umbrella                                   no -> Go Outside -> end
                                                            no  ->  Wait a While -> is raining?
                                                                                               yes -> wait a while (loop) -> and so on     
            Fig : flowchart

        Before learning Flow Chart Control 

    2.2 Need to Understand Boolean Values (Yes and No) options

        Boolean Data type
        Boolean Values: Boolean Values have only two values that is 
            TRUE  represent as True, yes, 1 and so on
            FALSE represent as False, no, 0 and so on

    2.3 Comparison Operators

        Comparison Operators compare two values and evaluates down to a single Boolean value.
        
        Operator    Meaning
        ==          Equal to
        !=          Not Equal to
        <           Less than
        >           Greater than
        <=          less than or equal to
        >=          Greater than or eual to

        This Operators evaluates to True and False depending on the values you give them...

        Can be use on any data types...

        The Difference Between == AND = operators
            The == operator (equal to) asks whether two values are the same as each other
            The = operator (assignment) puts the value on the right into the variable on the left

    2.4 Boolean Operators
        Binary Boolean Operators
        True and True is True
        True and False is False

        The and Operator's Truth table
        Expression      Evaluates to...
        True and True   True
        True and False  False
        False and True  False
        False and False False

        The or Operator;s Truth Table
        Expression      Evaluates to...
        True or True    True
        True or False   True
        False or True   True
        False or False  False

        The not Operator

        Not True => False
        
        not not not not True => True

        The not Operator's Truth Table

        Expression      Evaluates to...
        Not True        False
        Not False       True

    2.5 Mixing Boolean and Comaparison Operators
        ( 4 < 5 ) and ( 5 > 6 )
        True
        ( 4 < 5 ) and ( 9 < 6 )
        False
        ( 1 == 2 ) or ( 2 == 2 )
        True
        2 + 2 == 4 and not 2 + 2 == 5 and 2 * 2 == 2 + 2
        True

    2.6 Elements of Flow Control

        Flow control statement often starts with a part called the conditions,

        followed by block of code called the clause

        Consitions
            The Boolean expressions could all be considered as conditions which are same thing as expressions

            conditions is just a more specific name in contect of Flow control statement

            Conditions always evaluate down  to a Boolean value
                True
                False

            A Flow Control Statement decides what to do based on whether its condition is True or False
            and almost every flow control statement uses a condition


        Blocks of code
            lines of python code can be grouped together in Blocks
            from indentation of the line of code you can tell when block begins and {% endspaceless %}

                Three Rules for Blocks:
                1. Blocks begin when the indentation increases
                2. Blocks can contain other blocks
                3. Blocks end when the indentation decreases to zero or to a containing blocks indentation
            
            eg:

            if name == 'Bob':
                print('Who?')
            if password == 'swordfish':
                print('Access granted.')
            else:
                print('wrg password')

        Program Execution
            top to down, one after another 
            The program execution is a term for the current instruction being executed

    2.7 Flow Control Statements
        statements represent diamond you see in flow chart true / false, yes / no, etc


        if Statements  
            most common type of flow control statement 
            if statement clause ( the block following the if statement ) will execute 
            if the statement's condition is True.
            The clause is skiped if the condition is False

            in english terminology " If this condition is true, execute the code in the clause "

            if statement consists of :
                the if keyword
                A Condition ( an expression that evaluate to True or False )
                A colon
                Starting on the nexr line, an indented block of code ( called if clause )

                For example:
                    if name == 'Arbin':
                        print('Hi, Boss.')

            The flowchart for an if Statement

                                            True    ->  print('Hi, Boss.')   ->  end
                Start ->    name == 'Arbin' 
                                            False   ->  end

        else Statements
            of clause can optionally be followed by an else statement.
            else clause is executed only when the if statement's condition is False
            in english terminology
                If this condition is true, execute this code. Or else, execute that code

            else statement contains:
                The else keyword
                A colon
                Starting on the next line, an indented block of code ( else clause )

                for example:
                    if name == 'Arbin':
                        print('Hi, Boss.')
                    else:
                        print('Dare yo, Omaye wa!')
            
            The flow chart os this code looks like this

                                        True    ->  print('Hi, Boss.')  ->  end
            Start   ->  name == 'arbin' 
                                        False   ->  print('Dare yo, Omaye wa!') ->  end

        elif Statements
            While only one of the if or else clause will execute, you may have a case where you want one of many possible clauses to execute
            elif statements is an else if statement that always follows an if or another elif statement.
            It provides another condition that is checked only if any of the previous conditions were false, in code, an elif statement always consists of the following

            Contains:
                elif keyword
                a condition ( an expression that evaluates to True or False )
                A colon
                Starting on the next line, an indented block of code ( elif clause )

                for example:
                    if name == 'Arbin':
                        print('Hi, Arbin.')
                    elif age < 23:
                        print('You are not Arbin, Who!....')

            Flow chart


                                        True    ->  print('Hi, Arbin.') ->  end
            Start   ->  name == 'Arbin'                     True    ->  print("You are not Arbin, Who!....")    
                                        False   ->  age < 23     
                                                            False   ->  end


            name = 'ZiziManon'
            age = 101
            if name == 'arbin':
                print('Konichiwa, aniki.')
            # elif statements
            elif age < 23:
                print('Anatawa arbin janai desu ne. Dare!.....')
            elif age > 2000:
                print('Aniki wa bakemono ja nai desu!')
            elif age > 100:
                print('Aniki wa kono kusobaba za nai desu!')
            
            FlowChart for the above block of code



                                        True    ->  print('Konichiwa, aniki.')  ->  end
            Start   ->  name == 'arbin'                     True    ->  print('Anatawa arbin janai desu ne. Dare!.....')    ->  end
                                        False   ->  age < 23                        True    ->  print('Aniki wa bakemono ja nai desu!') ->  end  
                                                            False   ->  age > 2000                      True ->  print('Aniki wa kono kusobaba za nai desu!')    ->  end  
                                                                                    False   ->  age > 100
                                                                                                        False   ->  end


            if - elif - else
                name = 'Zion'
                age = 23
                if name == 'arbin':
                    print('Konichiwa, aniki.')
                # elif statements
                elif age < 23:
                    print('Anatawa arbin janai desu ne. Dare!.....')
                else:
                    print('Dare yo, Omaye wa!')
                    
            
            Flow Chart



                                    True    ->  print('Konichiwa, aniki.')  ->  end  
            Start -> name == 'Alice'                        True    ->  print('Anatawa arbin janai desu ne. Dare!.....')    ->  end    
                                    False   ->  age <   23
                                                            False   ->  print('Dare yo, Omaye wa!') ->  end  


        While Loop Statements

            You can make a block of code execute over and over again with a while statement.
            The code in a while clause will be executed as long as the while statements condition is True
            In code, a while statement always consists of the following:
                The while keyword
                A condition ( an expression that evaluates to True or False )
                A colon
                Starting on the next line, an indented block of code (called the while clause)

            for e.g 
                spam = 0
                while spam < 5:
                    print('Hello, world.')
                    spam = spam + 1

            Flow chart of example

                            True    -> print('Hello, world.')  ->  spam = spam + 1 -> spam = spam + 1
            start -> spam < 5
                            False   -> end

            
            An Annnpying while loop
            name = ''
            while name != 'your name':
                print("please type your name.")
                name = input()
            print('Thank You!')

            if you never enter your name string the while loops will never be false and program will keep on asking
            the input() call lets the user enter the right string to make the program move on.

            break statements
                it breaks out of a while loop clause early
                it immediatly exists when reaching the break statement
                break keyword

            e.g with break
                while True
                    Print('Your Name: ')
                    name = input()
                    if name == 'your name':
                        break
                print('Thanks')

            
            continue statements
                Like break, continue are used inside the loops
                when reaching to continue statement
                program execute immediatly jumps back to the start of the loop and reevaluates the loop's
                condition
                
                
            Trapped in an infinite loop??
                ctrl - c 

            e.g with break and continue Statements

            while True:
                print('Dare Desuka?')
                name = input()
                if name != 'zion':
                    continue
                print('Hello, zion-tan. passwordo wa nandei? (sakana desu!) ')
                password = input()
                if password == 'sakanaken':
                    break
                print('Access granted.')

            Truthy AND Falsey values
                when used in condition 0, 0.0, and '' the empty strings are considered as False, while all other values are considered True
                for e.g:
                name = ''
                while not name:
                    print('your name:')
                    name = input()
                print('how many guest will you have?')
                numofGuests = int(input())
                if numofGuests:
                    print('enough room for guest?')
                print('Down')
            
        For loops and the range() Function
            The while loop keeps looping while its condition is True.
            but what if you want to execute a block of code only a certain number of time.
            you can do this with a for loop statement and the range() function.

            for loop contains
                The for keyword
                A variable name
                The in keyword
                A Call to the range() method with up to three integers passed to it
                A colon
                starting on the next line, an indented block of code
                
            for example    
                print('Watashi no namaye')
                for i in range(5):
                    print('Manon Five Times (' + str(i) + ')')

                you can use brake and continue on for loops as well. only inside while and for loop


                total = 0 
                for num in range(101):
                    total = total + num
                print(total)
                
        An equivalent while loop
            you can use a while loop to do the same thing as a for loop
            for loops are more concise than while loops


        The Starting, Stopping and Stepping Arguments in range()

            Some functions can be called with multiple arguments seperated by a comma, and range() is one of them...
            This lets you change the integer passed to range() to follow any sequence of integers, including starting a number
            other than zero

            range(starting, stopping, stepping)

            for i in range(12, 16):
                print(i)

    2.8 Importing Modules
        All Python program can call a basic set of functions called:
            Built in functions
                Including print(), input(), and len()
        
        Python also comes with a set of modules called the standard library

        Each Module is a Python program that contains related group of functions that can be embedded in your programs.
        for e.g math module has mathematics related functions the random module has random number related functions and 
        so on.

            import statement consists of the following
                The import keyword
                The name of the module
                Optionally more module names as long as they are seperated by commas
            
                for e.g with random module
                import random
                for i in range(5):
                    print(random.randint(1, 10))
                
                random.randint() function call evaluates to a random integer value between the two integers that you pass it.
                most type random. to call the functions

                example of an import statement that imports four different Modules
                    import random, sys, os, math

            form import Statements
                An alternative form of the import statement is composed of from keyword followed by the modules name
                the import keyword and a star; for example

                    from random import *;
                    
                    this form of import statement calls to functions in random will not need the random. prefix. However using the full name makes for more
                    readable code 
                    so it is better ti use normal form of import statement that is without from keyword

    2.9 Ending a Program Early with sys.exit()
        The last flow control statement concept to cover is how to terminate the program

        for e.g 
        import sys

        while True:
            print('Type exit to exit.')
            response = input()
            if response == 'exit':
                sys.exit()
            print(' You typed ' + response + '.')   

    Practice Questions answers are in answers.py

    Extra credits look up 
        round()
        abs()


Chapter 3: Functions
    You can write your own functions.. like built in function such as print(), input(), len()

    A function is like a mini-program within a program
    Hello function
        def hello():
            print('howdy!')
            print('howdy!!')
            print('moshi moshi!')

        hello()
        hello()
        hello()

    the first line is a def statement, which defines a function named hello()
    the code in the block that follows the def statement is the body of the function.

    hello() lines after then function are function calls


    A major purpose of the function is to group code that gets executed multiple times.
    with out the function defined, you have to copy paste each time.
    and the program would look like this
        print('howdy!')
        print('howdy!!')
        print('moshi moshi!')print('howdy!')
        print('howdy!!')
        print('moshi moshi!')print('howdy!')
        print('howdy!!')
        print('moshi moshi!')

    you should always remove the duplicated code to make it better readable

    3.1 def Statement with Parameters
        def hello(name):
            print('konichiwa ' + name)

        hello('Zion')
        hello('Arbin')

        the definition of hello function has name Parameters
        the parameter is a variable that an arguments is stored in when a function is called
        when hello('Zion') function is called it is called with the argument 'Zion'
        the program execution enters the function and the variable name is set to the 'Zion' automatically

        so the print is printed as bellow
            konichiwa Zion
            konichiwa Arbin
    
    3.2 Return Values and return Statements
        The len() function and passing the argument such as 'hello'
        the function evaluates to the integer value 5,
        which is the length of the string you passed it
        the value that a function call evaluates to is called 
        the return value of the function
        

        Return statement is consisted of
            The return keyword
            the value or expression that the function should return

            for e.g 
            in magic8Ball.py

            1. Python first imports the random module.
            2. getAnswer() function is defined
            3. The program execution moves to the top of the getAnswer()
            4. The random.randint() function is called with two arguments, 1 and 9
            5. the getAnswer() function is called with r as argument
            5. the program ececution return to the line at the bottom of program that originally called getAnswer()
            6. the returned string is assigned to a variable named fortune, which then gets passed tp a print() call

            shorten the three lines after function to one single line for better readability and faster execution
            print(getAnswer(random.randint(1,9)))

    3.3 The None Values
        None is the value, which represents the absence of a value.
        None is the only value of the NoneType data types...
        Just like Boolean True and False values, None must be with a capital None

        Value-without-a-value can be helpful when you need to store something that won't be
        confused for a real value in a variable.
        One place where None is used is as the return value of print()

        for e.g
        spam = print('Hello')
        Hello!
        None == spam
        True

    3.4 Keyword Arguments and print()
        Most arguments are identified by their position in the function call. 
        for e.g 
            random.randint(1, 10) is different from (10, 1)
        The function call random.randint(1, 10) will return a random integer betn 1 and 10
        becuase the firsst argument is the low end of the range ang the second is the high end

        keyword arguments are identified by the keyword put before the function call
        keyword arguments are often used for optional parameters 
        for e.g
            print() have optional parameters such as end and sep which represents printed at end and what seperates them
            there are more such as file, flush with their specifiv features
            for end
            print('hello', end='')
            print('world')
                output is helloworld

            for sep
            print('cat', 'dog', sep=',')
                output is cat,dog

                Common Built in function
                Function,Most Important Optional Parameters,Description
                print(),"sep, end, file, flush",sep defaults to a space; end defaults to a newline (\n).
                sorted(),"key, reverse",key takes a function to customize sort logic; reverse is a boolean.
                enumerate(),start,Defines the beginning index (defaults to 0).
                round(),ndigits,The number of decimal places to round to.
                int(),base,"If the first argument is a string, you can specify the number base (e.g., 2, 8, 16)."
                open(),"mode, encoding, errors, newline",mode defaults to 'r' (read); encoding is vital for text files.
                range(),"start, step","If one arg is given, it's stop. If two, they are start, stop."
                getattr(),default,Prevents an AttributeError if the attribute doesn't exist.

                Categorized List of Built-ins
                There are 71 built-in functions in Python 3.12+. Here is a categorized overview:

                1. Data Type Conversions
                bool(x)

                dict(kwargs), dict(mapping, kwargs)

                float(x)

                int(x, base=10)

                list(iterable)

                set(iterable)

                str(object='', encoding='utf-8', errors='strict')

                tuple(iterable)

                2. Sequence & Iteration
                filter(function, iterable): Filters elements.

                map(function, iterable, ...): Applies function to every item.

                max(arg1, arg2, *args, key=None): Or max(iterable, default=obj, key=func).

                min(arg1, arg2, *args, key=None): Same signature as max.

                sum(iterable, start=0): Adds items of an iterable from left to right.

                zip(*iterables, strict=False): The strict parameter ensures all iterables are the same length.

                3. Object & Introspection
                dir([object]): Without arguments, returns the list of names in the current local scope.

                help([object]): Invokes the built-in help system.

                id(object): Returns the "identity" of an object.

                isinstance(object, classinfo): Checks if an object is an instance of a class.

                type(object) or type(name, bases, dict): Returns the type or creates a new type object.

                4. Mathematical
                abs(x): Absolute value.

                divmod(a, b): Returns (a // b, a % b).

                pow(base, exp, mod=None): Returns (baseexp) % mod.

    3.5 Local and Global Scope

        Parameters and variables that are assigned in a called function are said to exist in that functions local scope.
        Variables that are assigned out side all functions are said to exist in the global scope.
        The variable that exists in a local scope is called local variable
        while a variable that exists in the global scope is called global variable

        A variable must be one or the other it cannot be both local and global

        Scope is an container for variables
        when a scope is destroyed, 
        all the values stored in the scope variables are forgottern.
        There is only one global scope, and its created when your program begins.
        program terminates then the global scope is destroyed and all its variable are forgottern.
        Otherwise the next time you ran your program the variables would remember their values from the last time you ran it

        scope matters for several reasons:
            Code in the global scope cannot use any local variables
            However, a local scope can access global Variables
            Code in a function local scope cannot use variables in any other local scope
            You can use the same name for different variables if they are in different scopes.
            There can be a local variable named spam and a global variable also named spam.

            different scopes instead of just making every thing a global variable is so that when variable
            are modified by the code in a call to a function, the function interacts with the rest of the program
            only thaught its parameters and the return value 

            This narrows down the list code lines that may be causing

            Local Variables Cannot Be Used in the Global Scope
                def spam():
                    eggs = 34337
                    
                spam()
                print(eggs) this line will throw the error

            Local Scopes Cannot use variables in other local scope
                # def spam():
                #     eggs = 99
                #     bacon()

                # def bacon():
                #     ham = 101
                #     eggs = 0

                # spam()

                1. local variable eggs is set to 99
                2. bacon() function is called
                3. execution continues in the spam() function to print the value of eggs
                4. local variable ham is set to 101 and eggs which is different from spam()
                5. the spam() is called

            Global variables can be read from a local Scope

                # def spam():
                #     print(eggs)

                # eggs = 42
                # spam()
                # print(eggs)



Chapter 4: Lists


Chapter 5: Dictionaries and Structuring Data


Chapter 6: Manipulating Strings
